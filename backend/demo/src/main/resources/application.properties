spring.application.name=demo
spring.config.import=optional:classpath:application-secrets.properties

spring.datasource.url=jdbc:postgresql://localhost:5432/job_portal
spring.datasource.username=postgres
spring.datasource.password=Deekshi@260104

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT configuration
jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
jwt.expiration=86400000

# Google OAuth
oauth.google.client-id=${GOOGLE_CLIENT_ID:}
oauth.google.client-secret=${GOOGLE_CLIENT_SECRET:}

# GitHub OAuth — set GITHUB_CLIENT_ID and GITHUB_CLIENT_SECRET as environment variables.
# You can also paste the values directly here for local development, but avoid committing secrets.
oauth.github.client-id=${GITHUB_CLIENT_ID:}
oauth.github.client-secret=${GITHUB_CLIENT_SECRET:}

# -------------------------------------------------------
# Company Discovery Module Configuration
# -------------------------------------------------------
# SERP API key is stored in application-secrets.properties (excluded from Git)
# serpapi.key is loaded from there automatically via spring.config.import above.

# Configurable discovery search parameters
company.discovery.keywords=Software,IT,SaaS,AI,Cloud,FinTech
company.discovery.country=India
company.discovery.industry=Technology
company.discovery.max-results=100

# Email (SMTP) configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Frontend URL for emails (password reset links)
app.frontend.url=http://localhost:3000
spring.mail.username=deekshi26001@gmail.com
spring.mail.password=ehycuxzweddqdftw