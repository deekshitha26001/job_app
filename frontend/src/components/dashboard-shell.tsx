"use client";
import Link from "next/link";
import { usePathname, useRouter } from "next/navigation";
import { useEffect } from "react";
import { useAuth } from "@/context/AuthContext";
import { Bell, Bookmark, BriefcaseBusiness, LayoutDashboard, LogOut, Search, UserRound, Loader2 } from "lucide-react";
const items=[["Overview","/dashboard",LayoutDashboard],["Browse jobs","/dashboard/jobs",Search],["My applications","/dashboard/applications",BriefcaseBusiness],["Saved jobs","/dashboard/jobs",Bookmark],["Profile","/dashboard/profile",UserRound]];
export function DashboardShell({children}:{children:React.ReactNode}){const path=usePathname(),router=useRouter(); const { user, loading, logout } = useAuth(); useEffect(() => { if (!loading && !user) router.push("/login"); }, [user, loading, router]); if (loading || !user) return <div className="flex h-screen items-center justify-center"><Loader2 className="animate-spin text-violet-600" size={32}/></div>; return <div className="min-h-screen bg-zinc-50 text-zinc-900 dark:bg-zinc-950 dark:text-white"><aside className="fixed inset-y-0 hidden w-64 border-r border-zinc-200 bg-white p-5 md:block dark:border-zinc-800 dark:bg-zinc-900"><Link href="/" className="flex items-center gap-2 text-lg font-bold"><span className="grid h-8 w-8 place-items-center rounded-lg bg-violet-600 text-white"><BriefcaseBusiness size={17}/></span>Hirely</Link><nav className="mt-10 space-y-1">{items.map(([name,href,Icon])=>{const I=Icon as typeof Search;return <Link key={name as string} href={href as string} className={`flex items-center gap-3 rounded-lg px-3 py-2.5 text-sm font-medium ${path===href?"bg-violet-50 text-violet-700 dark:bg-violet-950 dark:text-violet-300":"text-zinc-500 hover:bg-zinc-100 dark:hover:bg-zinc-800"}`}><I size={18}/>{name as string}</Link>})}</nav><button onClick={logout} className="absolute bottom-6 flex items-center gap-3 px-3 text-sm text-zinc-500"><LogOut size={18}/>Log out</button></aside><div className="md:pl-64"><header className="flex h-16 items-center justify-between border-b border-zinc-200 bg-white px-5 dark:border-zinc-800 dark:bg-zinc-900"><div className="hidden max-w-sm flex-1 items-center gap-2 rounded-lg bg-zinc-100 px-3 py-2 text-sm text-zinc-500 sm:flex dark:bg-zinc-800"><Search size={16}/>Search jobs</div><div className="ml-auto flex items-center gap-4"><button aria-label="Notifications" className="relative text-zinc-500"><Bell size={19}/><span className="absolute -right-1 -top-1 h-2 w-2 rounded-full bg-violet-600"/></button><Link href="/dashboard/profile" className="grid h-8 w-8 place-items-center rounded-full bg-violet-200 text-xs font-bold text-violet-700">{user?.email?.charAt(0).toUpperCase() || "U"}</Link></div></header><main className="p-5 lg:p-8">{children}</main></div><nav className="fixed inset-x-0 bottom-0 z-20 flex justify-around border-t border-zinc-200 bg-white py-2 md:hidden dark:border-zinc-800 dark:bg-zinc-900">{items.slice(0,4).map(([n,h,I])=>{const Icon=I as typeof Search;return <Link href={h as string} key={n as string} aria-label={n as string} className="p-2 text-zinc-500"><Icon size={20}/></Link>})}</nav></div>}
