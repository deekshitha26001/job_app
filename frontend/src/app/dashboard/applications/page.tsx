import { applications } from "@/lib/mock-data";
export default function Applications(){return <div className="mx-auto max-w-5xl"><p className="text-sm font-medium text-violet-600">YOUR SEARCH</p><h1 className="mt-1 text-3xl font-semibold tracking-tight">My applications</h1><p className="mt-2 text-zinc-500">Keep track of every opportunity in one place.</p><div className="mt-8 space-y-3">{applications.map(a=><article key={a.id} className="flex items-center gap-4 rounded-2xl border border-zinc-200 bg-white p-5 dark:border-zinc-800 dark:bg-zinc-900"><div className={`grid h-12 w-12 place-items-center rounded-xl font-bold text-white ${a.job.company.color}`}>{a.job.company.initials}</div><div className="flex-1"><h2 className="font-semibold">{a.job.title}</h2><p className="mt-1 text-sm text-zinc-500">{a.job.company.name} · Applied {a.appliedAt}</p></div><span className="rounded-full bg-violet-100 px-3 py-1.5 text-xs font-semibold text-violet-700">{a.status}</span></article>)}</div></div>}
