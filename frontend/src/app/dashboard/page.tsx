import Link from "next/link";
import { ArrowUpRight, Bookmark, CalendarDays, Eye, Send } from "lucide-react";
import { applications, jobs, user } from "@/lib/mock-data";
import { JobCard } from "@/components/job-card";
const stats=[["Applications sent","12","+20%",Send],["Interviews","3","This week",CalendarDays],["Saved jobs","24","+8 this month",Bookmark],["Profile views","86","+14%",Eye]];
export default function Dashboard(){return <div className="mx-auto max-w-6xl"><p className="text-sm font-medium text-zinc-500">Tuesday, July 15</p><h1 className="mt-1 text-3xl font-semibold tracking-tight">Good morning, {user.name.split(" ")[0]}.</h1><p className="mt-2 text-zinc-500">Here’s what’s happening with your search.</p><div className="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{stats.map(([name,value,trend,Icon])=>{const I=Icon as typeof Send;return <div key={name as string} className="rounded-2xl border border-zinc-200 bg-white p-5 shadow-sm dark:border-zinc-800 dark:bg-zinc-900"><I size={18} className="text-violet-600"/><p className="mt-5 text-2xl font-semibold">{value as string}</p><p className="mt-1 text-sm font-medium">{name as string}</p><p className="mt-2 text-xs text-emerald-600">{trend as string}</p></div>})}</div><div className="mt-10 flex items-end justify-between"><div><h2 className="text-xl font-semibold">Recommended for you</h2><p className="mt-1 text-sm text-zinc-500">Based on your profile and saved roles.</p></div><Link href="/dashboard/jobs" className="text-sm font-semibold text-violet-600">Browse all</Link></div><div className="mt-5 grid gap-4 lg:grid-cols-2">{jobs.slice(0,2).map(job=><JobCard key={job.id} job={job}/>)}</div><div className="mt-10"><h2 className="text-xl font-semibold">Application activity</h2><div className="mt-5 overflow-hidden rounded-2xl border border-zinc-200 bg-white dark:border-zinc-800 dark:bg-zinc-900">{applications.map(a=><div className="flex items-center gap-4 border-b border-zinc-100 p-4 last:border-0 dark:border-zinc-800" key={a.id}><div className={`grid h-10 w-10 place-items-center rounded-xl text-sm font-bold text-white ${a.job.company.color}`}>{a.job.company.initials}</div><div className="min-w-0 flex-1"><p className="font-medium">{a.job.title}</p><p className="text-sm text-zinc-500">{a.job.company.name} · {a.appliedAt}</p></div><span className={`rounded-full px-3 py-1 text-xs font-semibold ${a.status==="Interview"?"bg-violet-100 text-violet-700":a.status==="In review"?"bg-amber-100 text-amber-700":"bg-sky-100 text-sky-700"}`}>{a.status}</span></div>)}</div></div></div>}
